package com.tufplus.arrays.hard.MajorityElement

object MajorityElement {
/*
  def findMajorityElement(arr: Array[Int]) = {
   val map = arr.foldLeft(Map[Int, Int]()) { case (eleMap, el) =>
     if (eleMap.contains(el)) eleMap ++ (el -> (eleMap(el) + 1))
     else eleMap ++ (el -> 1)
     eleMap
   }.toList.sortBy(_._2).findLast { case (num, numOfTimes) => numOfTimes > arr.length / 2 }
  }*/
}
